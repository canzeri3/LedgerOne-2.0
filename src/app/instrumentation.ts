// src/app/instrumentation.ts
// TEMP: Sentry is disabled in this build to allow production deploy
// without requiring @sentry/nextjs. This does NOT affect app logic,
// only error reporting.

export async function register() {
  // no-op for now
}
